%nav.navbar.navbar-default{:role => "navigation"}
  / Brand and toggle get grouped for better mobile display
  .navbar-header
    %button.navbar-toggle{"data-target" => "#navbar-collapse-1", "data-toggle" => "collapse", :type => "button"}
      %span.sr-only Toggle navigation
      %span.icon-bar
      %span.icon-bar
      %span.icon-bar
    = link_to "FS Rover", defined?(root_path) ? root_path : "#", class: "navbar-brand"
  / Collect the nav links, forms, and other content for toggling
  #navbar-collapse-1.collapse.navbar-collapse
    %ul.nav.navbar-nav
    %ul.nav.navbar-nav.navbar-right
      %li.dropdown
        %a.dropdown-toggle{"data-toggle" => "dropdown", :href => "#"}
          Account
          %b.caret
        %ul.dropdown-menu
          %li
            = link_to session[:username], '#', class: 'disabled'
          %li.divider
          %li= link_to "Sign Out", login_path, method: :delete
